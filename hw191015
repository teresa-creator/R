for (i in 1:9) {

  for (j in 1:i) {
    m=j*i
    cat(i,'*',j,'=',m,' ')
   }
   cat('\n')
}
var=function(x){
  result=0
  for(i in 1:length(x)){
    result=result+(x[i]-mean(x))**2
  }
  result=result/(length(x)-1)
  return(result)
}
var(1:10)
